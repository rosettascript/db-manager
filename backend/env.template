# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Encryption Key (generate a secure random key)
# Use: openssl rand -base64 32
ENCRYPTION_KEY=change-this-to-a-secure-random-key

# Database Connections Storage Path (relative to backend directory)
CONNECTIONS_FILE_PATH=./database/connections.json
